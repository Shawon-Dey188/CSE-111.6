#include <stdio.h>

int main() 
{
    float mark;
    printf("Enter the mark= ");
    scanf("%f", &mark);
    
  
    if (mark >= 60 && mark <= 100) {
        printf("You passed the exam.\n");
    } else {
        printf("Failed\n");
    }
    


    return 0;
    }
