#include <stdio.h>

int main() {
    int i,n, sum = 0;
    printf("Enter a value for n: ");
    scanf("%d", &n);

    for (i = 1; i <= n; i=i+2) 
    sum=sum+i;
    printf("Sum of odd numbers between 1 and %d is: %d\n", n, sum);
    return 0;
}
